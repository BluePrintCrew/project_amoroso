spring:
  datasource:
    url: jdbc:h2:file:C:/Users/guswp/amoroso_test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console
  jwt:
    secret: your_test_secret_key_should_be_at_least_32_bytes_long_for_hs256_algorithm