spring:
  application:
    name: AmorosoBackend
  datasource:
    url: jdbc:mysql://localhost:3306/amoroso_test
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
